#ifndef _throw_
#define _throw_

int throw_once_more(int overlap, int yoot)
{
   int more = 0;
   
   if(overlap == 1)
   {   
      more++;
   }
   else if(yoot == 4 || yoot == 5)
   {
      more++;
   }
   
   return more;
}
#endif